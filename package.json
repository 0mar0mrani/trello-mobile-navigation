{
  "name": "trello",
  "version": "1.0.0",
  "description": "Trello Mobile Navigation Copy",
  "main": "index.js",
  "scripts": {
    "watch:sass": "sass --watch sass/main.scss css/style.css",
    "devserver": "live-server --browser='Google Chrome'",
    "start": "npm-run-all --parallel devserver watch:sass"
  },
  "author": "Omar Omrani",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.56.1"
  }
}
